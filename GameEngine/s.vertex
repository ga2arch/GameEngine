#version 330

in vec3 position;
in vec3 normal;

uniform mat4 proj;
uniform mat4 view;
uniform mat4 model;

out vec3 norm;

void main() {
    norm = normal;

    gl_Position = proj * view * model * vec4(position, 1.0);
}